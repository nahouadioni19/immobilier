package com.app.controller.abonne;

/*import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;

import com.app.entities.administration.Agence;
import com.app.security.UserPrincipal;

@Controller
@RequestMapping("abonnements")
public class AbonnementController {

	@GetMapping("/renouveler")
	public String renewPage(@AuthenticationPrincipal UserPrincipal principal, Model model) {
	    model.addAttribute("agence", principal.getUtilisateur().getAgence());
	    return "abonnement/renouveler";
	}
		
}*/
